// ltl-trie/main.ltl
//
// Compressed prefix trie with range queries.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-trie v0.1.0")
    io::println("Compressed prefix trie with range queries.")
    0
}
