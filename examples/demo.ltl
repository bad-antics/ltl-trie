// examples/demo.ltl — runnable demo for ltl_trie

import std::io
use ltl_trie as lib

fn main() {
    io::println("ltl_trie demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}